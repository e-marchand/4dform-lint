# `no_overlap`

Default severity: `error`

Flags two elements on the same page when their frames intersect.

Use this to catch accidental stacking, hidden controls left in place, or edits that shifted one control into another.

Suppress an intentional overlap with `ignore_rules: [no_overlap]` on one of the elements.
